<table>
<?php
if (is_array($this->nodes)){
    foreach ($this->nodes as $node) {
        echo '<tr><td><a href="'.$this->url(array('controller'=>'objects', 'action'=>'delete','nodeId'=>$node->nodeId)).'">x</a></td><td>'.$node->nodeName . '</td><td>' . $node->parentNodeId . '</td></tr>';
    }
}
?>
</table>
