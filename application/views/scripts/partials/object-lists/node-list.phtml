<div id="objects-list">
    <?php
    if (is_array($this->nodes)) {
        foreach ($this->nodes as $node) {
            echo '<div id="object"><div id="delete-object-icon"><a href="' . $this->url(array('controller' => 'objects',
                'action' => 'delete',
                'objectType' => 'node',
                'nodeId' => $node->nodeId), null, true) .
            '">x</a></div><div id="object-name"><a href="' . $this->url(array('controller' => 'objects',
                'action' => 'open-object',
                'objectType' => 'node',
                'nodeId' => $node->nodeId)) .
            '">' . $node->nodeName .
            '</a></div><div id="parent-object">' . $node->parentNodeId .
            '</div></div>';
        }
    }
    ?>
    <div style="display: block; clear: both;"></div>
</div>
